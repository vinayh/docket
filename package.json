{
  "name": "docket",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "docket": "bun src/cli/index.ts",
    "migrate": "bun src/db/migrate.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage --coverage-reporter=lcov --coverage-dir=coverage",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.1.42",
    "@types/diff": "^8.0.0",
    "drizzle-kit": "^0.31.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "diff": "^9.0.0",
    "drizzle-orm": "^0.45.2"
  }
}
